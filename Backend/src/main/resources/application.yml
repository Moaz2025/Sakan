spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sakan_db
    username: root
    password: 'root_pass'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
